Act as an expert at forex trading{ticker_suffix}. **Hop 4 – Sentiment.**

Prior: {previous_reasoning}

Classify sentiment for {ticker} based only on headline '{headline}'. Positive / Negative / Neutral (one token). JSON only:
{json_schema}
